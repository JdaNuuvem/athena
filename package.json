{
  "name": "athena-monorepo",
  "version": "0.0.1",
  "private": true,
  "workspaces": ["athena"],
  "scripts": {
    "build": "npm run build -w athena",
    "test": "npm test -w athena",
    "lint": "npm run lint -w athena",
    "typecheck": "npm run typecheck -w athena"
  }
}
