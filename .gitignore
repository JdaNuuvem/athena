node_modules
.playwright-mcp/

# Segredos — nunca versionar
.env
.env.*
!.env.*.example
hermes_agents/config/api_config.json

# Bytecode Python
__pycache__/
*.pyc

# Saida gerada pela skill /graphify — regeneravel, nao e' codigo fonte
graphify-out/

# Scripts de debug/deploy soltos na raiz (nao sao parte do codigo do projeto)
/_*.py

# Scripts de debug/deploy soltos, agora organizados em /scripts
/scripts/*.py

# Arquivos-lixo (sobras de redirecionamento de shell por engano)
/1
/data
/dict
/fill
/hermes_agents/'
/hermes_agents/,
/hermes_agents/,+
/hermes_agents/200
/hermes_agents/302
/hermes_agents/ERRO
/hermes_agents/ERRO'
/hermes_agents/bool
/hermes_agents/catalogo
/hermes_agents/dict
/"shopee
/web/,-
/'
/([])
/0
/1
/3
/bool
/conversao
/deve
/dict
/list
/max_pct
/s.id
/str
/Operadores
/CURRENT_DATE
/NOW()

# hermes_agents/dashboard/ (export estatico do Next.js) e' 100% gerado pelo
# Dockerfile a cada build (npm run build com NEXT_STATIC_EXPORT=true dentro
# do container) — versionar so' gera diffs gigantes sem beneficio real.
/hermes_agents/dashboard/

# Worktrees isolados de features em desenvolvimento (superpowers) — scratch,
# nao e' codigo fonte.
/.claude/worktrees/
